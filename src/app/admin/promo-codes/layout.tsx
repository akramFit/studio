
import React from 'react';

// This layout is needed for the route group to work correctly.
export default function PromoCodeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
